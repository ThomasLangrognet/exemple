//ici j'exporte juste modelsFunctions lié à mongoDB vers l'index.js plus haut qui lui est indépendant de mongoDB

const modelsFunctions = require('./functions');

module.exports = {
  modelsFunctions
};
